# Classroom Tasks 21–24 실행 및 1–24 전체 검증

Task 21–24의 저장소 구현과 1–24 전체 재점검을 완료했다. 최종 리뷰의 남은 지적은 0개이며 로컬·Linux 브라우저 44개가 각각 통과했다. **Python은 메인 Backend로 유지하며 보호 대상 70개 파일의 변경은 0개다.** 실제 Replit 배포와 최신 main 통합은 이 통과 결과에 포함하지 않는다.

- 브랜치: `codex/learning-tasks-21-24`
- 작업공간: `/Users/rinny/IdeaProjects/Mentor-Connect-sdd-learning-11-15`
- 이번 시작점: `5fb771b43832f7fdf75199c52d534a396f72e912`
- 전체 비Python 구현 시작점: `98e1b04c292302bd25365ed7db4c5675671df337`
- 실행 방식: 독립 구현·검토 작업은 subagent로 병렬화하고, 공유 파일과 브라우저 서버는 순서대로 인계했다. 원본 checkout은 수정하지 않았다.

## 이번 변경

| 범위 | 변경과 목적 | 구현 commit |
| --- | --- | --- |
| Task 21 | 세션 확인 이후 로그인·가입 이동, 모든 401에서 이전 계정 제거, 로그아웃 실패 시 초안 유지, 계정별 캐시·요청 취소, 빈 bio 저장 | `04cc5b46`, `58f3ffdb` |
| Task 22 | HTTP 오류·학습 미완성·성공한 빈 결과를 구분하고 안전한 오류 안내와 재시도 제공. 요청·Connect·채팅의 서버 상태 확인, 접근성 보완 | `852e682f`, `d9dc8b07` |
| Task 23 | 실제 빌드 gateway를 통과하는 브라우저 동선, 두 계정 채팅, 배포용 HTTPS smoke 도구와 별도 실제 Python 검증 | `0279d491`, `0849c2f4` |
| Task 24 | 공통 release 검증 명령, CI·의존성 감사, SPA 경로 설정, 지원되는 CSP meta와 배포·rollback 운영 문서 | `adbc75fe` |
| Task 16 재점검 보완 | 실패 응답의 중첩 data/result/matches 아래 진단 정보도 제거하고 부모 실패 상태를 재귀적으로 유지 | `cb1fed52`, `31099d34` |
| 최종 리뷰 보완 | 채팅 목록 갱신 실패 안내·재시도와 초안 보존, Practice 성공 fixture·화면 검증 보강 | `b7c409c8` |

TypeScript에 Python의 매칭·채팅·DB 업무 로직을 새로 구현하지 않았다. 브라우저용 synthetic fixture는 테스트에서만 사용한다. Chat의 정상 조회는 5초, 일반 통신 실패 후 조회는 10초이며 429는 Retry-After를 따른다. 10초 조회 간격을 지수형 backoff라고 주장하지 않는다.

## 1–24 적용 상태

`로컬 통과`는 코드와 로컬 검증 범위를 뜻한다. 운영자가 실제 환경에서 수행해야 하는 확인까지 완료했다는 뜻은 아니다. 과거의 기능 차단은 사용자의 전체 학습 기능 공개 요청으로 대체되었다.

| 번호 | 판정 | 현재 적용과 확인 근거 |
| --- | --- | --- |
| 1 | 승인된 공개 정책으로 대체 | 모든 release flag가 true. production route·데스크톱/모바일 navigation 검사 |
| 2 | 로컬 통과, 배포 경계 미확인 | gateway만 공개 API/WS 진입점, Python loopback 8181. 라우팅·경계 검사 |
| 3 | 승인된 공개 정책으로 대체 | Reports를 인증된 최소 응답으로 공개. 개인정보·진단 정보 projection 검사 |
| 4 | 로컬 통과 | 타인 profile은 최소 네 필드만 조회, PATCH는 본인만 허용. self-only 검사 |
| 5 | 승인된 공개 정책으로 대체 | matching·practice·Connect 공개, Python 결과 사용과 비용 큰 호출 제한 검사 |
| 6 | 승인된 공개 정책으로 대체 | room/DM REST와 두 WS 경로 공개. TODO 유지 및 실제 Python room REST/WSS 확인 |
| 7 | 저장소 검사 통과, 외부 조치 미확인 | secret scanner·workflow 보호. 과거 credential 회전·계정 reset 증거는 incident ledger의 BLOCKED/UNKNOWN 유지 |
| 8 | 로컬 통과 | merge/startup DDL 없음. 명시적 migration 진입점·dry-run·감사 기록 검사 |
| 9 | 로컬 통과 | canonical schema·0001·checksum ledger·advisory lock·catalog 검사 |
| 10 | 로컬 통과, 실제 데이터 검증 별도 | 0002 불변, disposable preflight·constraint diff·query plan 검사. provider p95는 미확인 |
| 11 | 로컬 통과 | Node/pnpm 및 외부 Python runtime 고정, runtime/freeze 자체검사, 보호 파일 70개 해시 비교 |
| 12 | 저장소 검사 통과, 외부 조치 미확인 | 합성 secret fixture·minimumReleaseAge 1440 유지. 실제 SESSION_SECRET·과거 credential 회전은 별도 |
| 13 | 로컬 통과 | 빈 DB bootstrap·동시 실행·target/TLS·0600 credential guard. 변경 없는 실제 Python 가입/로그인/seed 확인 |
| 14 | 로컬 통과, provider 저장소 별도 | 일관 snapshot·checksum·빈 다른 target guard. 13개 테이블 restore와 ledger/catalog 일치 |
| 15 | 로컬 통과 | REST 48개 literal allowlist, canonical 경로·query·session·본인 수정 제한 |
| 16 | 재점검 보완 후 로컬 통과 | OpenAPI/generated 계약과 안전한 응답. 중첩 실패 진단 누출 수정·회귀 검사, production/full 감사 각각 0건 |
| 17 | 로컬 통과, 단일 instance 확인 별도 | exact HTTPS origin·입력/시간/크기 제한·사용자별 limiter·Retry-After 검사 |
| 18 | 로컬 통과, provider WS 별도 | 두 native tunnel, 인증·Origin·handshake·backpressure·종료 검사. 실제 Python WSS room 저장/상대 조회 확인 |
| 19 | 로컬 통과, 운영 credential 별도 | health·read-only DB·ledger readiness, 최소 로그·종료 처리. 기존 실제 PostgreSQL probe 기록 유지 |
| 20 | 로컬 통과 | 전체 production 화면·메뉴·Reports·member profile 포함. route heading과 필수 API 성공 확인 |
| 21 | 로컬 통과 | 인증 race·401 이후 503·A/B cache·로그아웃 실패·프로필 초안/빈 bio 회귀 검사 |
| 22 | 로컬 통과 | 학습/빈/통신 오류 구분, 실제 Connect 상태, chat TODO/429/취소, 안전한 Reports 오류 검사 |
| 23 | 로컬 통과, hosted smoke 별도 | 최종 브라우저 44개 및 별도 실제 Python HTTPS 30개 검사. 실제 배포 origin·private credential 파일 미제공 |
| 24 | 저장소·로컬/Linux 검증 통과, 배포 증거 별도 | CI/local 공통 명령·감사·SPA·CSP·운영 문서 구현. 실제 CI/PR/provider 배포 증거는 별도 |

## 검증 기록

`0279d491`에서 로컬과 새 Linux clone의 `pnpm verify:release`가 모두 exit 0으로 완료됐다. Node 24.21.0·pnpm 10.33.0을 사용했고 PostgreSQL은 macOS 16.13, Linux 16.15였다. Linux는 외부 포트를 열지 않은 disposable 컨테이너에서 일반 사용자로 실행했다. 실제 GitHub Actions 실행 결과는 아니다.

두 환경 모두 frozen install, runtime/freeze 자체검사 16개, 전체 typecheck·production build, gateway 87개+shield 8개, migration 보호, DB 56개+운영 명령 4개, UI unit 3개, codegen·generated drift 0, REST 48/WS 2 계약, 경계 154개, 추적 파일 486개 secret scan 및 scanner/history/workflow 자체검사, smoke 자체검사 3개, Chromium 40개, 두 로컬 freeze 기준 검사를 통과했다. 로컬 Chromium은 1.2분, Linux는 1.7분이었다.

공통 DB 명령에서 선택적 실제 Python 검사 1개는 명시적으로 생략했다. 별도로 외부 Python runtime을 지정한 아래의 57개 결과와 구분한다. Generator가 package-root export 한 줄을 canonical multiline으로 바꾼 것은 `0ac9635f`에 보존했다. export 이름과 동작은 동일하고 생성 디렉터리의 drift는 0이다.

smoke 도구 보완 후 `0849c2f49654b9536c9deac157436fb46c442714`에서도 두 환경의 공통 명령 전체를 다시 통과했다. 브라우저는 **42/42**, Node smoke 자체검사는 **4/4**, 추적 파일 secret 검사는 487개로 늘었고 나머지 결과는 같다. macOS 브라우저 1.2분, Linux 1.6분이었다. 이후 결과 문서와 JSON을 포함한 492개 추적 파일에서도 secret 검사가 통과했다.

마지막 UI·fixture 보완 commit은 **`b7c409c827b33e15e2880a6da68fb0d7b0817071`**이다. 동일한 코드에서 로컬 전체 typecheck·release build와 **브라우저 44/44(1.2분)**가 통과했다. 이 commit을 Linux clone에 반영한 뒤 release build·전체 typecheck, UI unit 3개, **브라우저 44/44(1.7분)**, 두 freeze 기준, client/generated 디렉터리 diff 0을 확인했다. 마지막 변경과 무관한 DB·gateway·계약·CI 검사는 앞의 `0849c2f4` 공통 명령 결과를 사용한다. 이 구분은 [통합 검증 JSON](2026-09-11-release-verification.json)에도 기록했다.

최종 commit으로 로컬 release를 다시 빌드한 뒤 실제 Python HTTPS 검사도 재실행했다. 아래의 소스·산출물 해시는 이 마지막 실행의 값이다. 이후 commit은 검증 문서와 JSON 정리만 포함한다.

- 수정하지 않은 Python을 연결한 전체 disposable DB suite: **57개 통과, 생략 0개**, 운영 명령 4개. preflight 위반 0, constraint 차이 0, query plan 6/6. 13개 테이블 복원 후 ledger·catalog 일치, 로컬 복원 60ms. 이 수치는 provider RTO가 아니다.
- runtime/freeze 자체검사 **16개 통과**. 구현 시작점 98e1b04와 이번 시작점 5fb771b4 대비 보호 파일 변경 0.
- 최초 production 브라우저 **40/40**에서 smoke 보완 후 42개, 최종 보완 후 **44/44**로 확장됐다. 마지막 구성은 auth 19, 실제 gateway 동선 5, 학습 상태 10, 공개 화면 8, redirect 2다. worker 1, retries 0. [최초 40개 기록](2026-09-11-classroom-browser-baseline.json)은 원래 소스와 결과를 그대로 유지한다.
- 최종 smoke/fixture 자체검사 **4/4 통과**.
- 2026-09-11 의존성 감사: production 180개 및 전체 399개 dependency 범위에서 각각 모든 severity **0건**, 두 명령 exit 0. [production JSON](../../security/dependency-audit-2026-09-11.production.json), [전체 JSON](../../security/dependency-audit-2026-09-11.complete.json).

### 실제 Python HTTPS 검증

최종 소스 및 gateway release metadata는 `b7c409c827b33e15e2880a6da68fb0d7b0817071`로 일치했다. 전체 gateway dist 해시는 `ae63c3b58ac46f68d3b352dd04ca659292e3619281ee8d215a23e2f479fb4848`, frontend dist 해시는 `99818861cbd772e6b530f2d66170130ed0d8ca5e1e51df00fa6a535d18688465`다. 정렬된 상대경로 + NUL + 각 파일 SHA-256 + LF를 이어 SHA-256으로 계산했다. 실행한 smoke script SHA-256도 `592c188cef7441c8ee8733949e66a062af3d19625080a1496e07e09db53e470e`로 별도 기록했다. [최종 실제 Python JSON](2026-09-11-classroom-python-smoke.json)에 각 검사의 상태·시간·산출물 출처를 보존했다.

이전 gateway 번들로 실행된 예비 기록은 폐기 대상으로 표시했고 위의 새 빌드로 반복한 결과만 근거로 사용한다. 공개 원격 smoke CLI는 정상 인증서 검증을 사용한다. 로컬 임시 harness만 자체 생성 인증서의 SPKI를 한 browser process에 허용했으며 전역 신뢰 설정을 바꾸지 않았다.

**30개 결과: 정상 20, learning-incomplete 10, transport-failure 0.** 두 계정 UI 로그인·본인/타인 최소 profile·self PATCH·districts·request 생성/조회/삭제·room REST 저장/상대 조회·native WSS 저장 후 상대 GET에서 정확히 한 번 확인·DM 시작·matching·analysis/scheduling import status가 정상이다.

| 학습 미완성 항목 | 실제 Python의 응답 |
| --- | --- |
| DM history | Mission 7 TODO |
| DM native WSS | Mission 8 TODO 후 정상 close |
| Analysis weekly matches | 학생 함수 없음 |
| Analysis popular subjects | 학생 runtime error |
| Scheduling popular time slots | 학생 runtime error |
| Analysis mentor response rates | 학생 runtime error |
| Scheduling overview | 학생 runtime error |
| Scheduling suggestions | 학생 runtime error |
| Reports summary | 기존 Python reports syntax error, 기존 adapter-fallback 표시 |
| Reports flagged users | 기존 get_blocks runtime error, 기존 adapter-fallback 표시 |

학습 미완성을 성공으로 바꾸거나 숨기지 않았으며 Python도 수정하지 않았다. sanitized 응답 metadata·request ID·HTTP 상태·시간만 별도 JSON에 보관한다. credential·cookie·메시지 본문·원시 오류·실사용 trace는 저장하지 않는다. smoke가 만든 request만 API로 삭제하며 삭제 API가 없는 room canary/DM은 원격 실행 시 남을 수 있다. 이번 로컬 실행에서는 소유한 임시 DB와 함께 정리했다.

### 개별 review 보완

Task 21의 자동 401 후 503에서 이전 계정이 다시 보이는 문제, Task 22의 Reports 오류 안내 누락, Task 16의 중첩 진단 정보 누출은 각 담당자가 보완하고 재검토 승인을 받았다.

Task 23 검토에서는 검증 도구 자체의 두 문제가 발견됐다. Chromium이 307/308 응답을 따라 허용 origin 밖으로 로그인 정보를 전송할 수 있는 경우를 smoke 전용 request/response guard로 차단했다. 별도 synthetic browser 회귀에서 외부 서버가 받은 credential body가 각 1개에서 0개로 줄었고 navigation도 차단됐다. 기존 fixture를 새 child로 오인하는 문제는 child가 listen 성공 후 보내는 IPC 신호를 기다리도록 바꿨다. 기존 sentinel에 도달한 요청은 4개에서 0개가 됐다. Node 자체검사 4개와 새 브라우저 검사 2개가 통과했고 guarded 실제 Python HTTPS도 다시 통과했다.

Task 21·22·23·24와 Task 16 보완은 각각 spec·품질 검토를 통과했다. 전체 변경에 대한 한 번의 독립 최종 리뷰에서는 Critical 0개, Important 0개, Minor 2개가 나왔다. 마지막 두 항목은 기존 채팅 목록을 유지하면서 갱신 실패를 안내하는 부분과, Practice 성공 fixture에 실제 Python 응답의 필수 message를 추가하고 성공 화면까지 확인하는 부분이었다. `b7c409c8`의 한 번의 수정 작업에서 둘 다 보완했다. 집중 회귀 3개가 수정 전 실패하고 수정 후 통과했으며, 한 번의 scoped 재검토에서 **Critical·Important·Minor 모두 0개, spec·품질 PASS**로 확정했다.

## 보존·정리

최종 보호 파일 해시 비교, 원래 구현 시작점과 이번 시작점의 freeze, migration/schema 불변, generated drift 0과 diff 공백 검사를 확인했다. 최종 기록을 포함한 secret 검사도 수행했다. 실행 과정의 tooltip sourcemap 진단 및 색상 환경 변수 경고는 기존 비치명 경고이며 검사 실패로 이어지지 않았다.

검증에 사용한 임시 DB·서버·TLS key/certificate·credential·private log는 각 harness가 제거했다. synthetic browser 결과와 이번 Linux 컨테이너·Git bundle·임시 harness/log를 정리했다. 이 계획의 scratch는 결과와 모든 판단을 이 문서와 JSON에 commit한 후 삭제한다. 외부 Python runtime과 기존 브랜치·worktree는 보존한다.

## 실제 배포·통합에서 남은 확인

- Replit project·공개 HTTPS origin·배포 계정이 제공되지 않아 실제 배포를 수행하지 않았다. instance 수 1, 외부 API/두 WS, Secure session cookie, readiness, SPA deep link·missing asset 404, 실제 ingress CSP/HSTS/Referrer/Permissions header와 alternate port 8181/8000/8081/21288 격리는 [classroom 운영 문서](../../runbooks/classroom-release.md)에 따라 확인해야 한다.
- CSP meta는 브라우저가 지원하는 지시어만 적용했다. meta로 지원되지 않는 frame-ancestors나 HSTS를 적용했다고 주장하지 않는다.
- 실제 DB read-only credential·SESSION_SECRET·과거 유출 credential 회전·백업 저장/보관/복원 증거는 해당 운영 환경의 후속 확인이다. 저장소 검사 통과로 incident ledger를 완료 처리하지 않았다.
- 로컬 `origin/main`은 `6de8e8fa`, merge-base는 `2fbf0a93374594b0f56dcc4e48a31c5db9970d7a`였다. 그 비교에는 98e1b04 이전에 추가된 `Python/routers/chat_Answer.py`가 잡혀 freeze 검사가 실패한다. 이번 구현 및 전체 비Python 구현 시작점 대비는 변경 0이다. **실제 PR이 없으므로 5fb771b4를 사용한 로컬 검증은 실제 PR-base 통과 증거가 아니다.** Python을 변경해 이 차이를 없애지 않았으며 최신 main 통합 후 실제 PR 기준 검증이 별도로 필요하다.
- 브랜치·worktree는 보존한다. 원본 checkout에 merge하거나 remote push하지 않았다.

## 실행 판단과 비용

아래는 이번 실행의 `Ruling:`을 빠짐없이 보존한 것이다.

Ruling: Reuse the existing isolated worktree on a new branch — it contains all reviewed 11–20 changes and avoids the unrelated dirty checkout — cost if wrong: integration must be redone in the desired checkout.

Ruling: Honor the user's continuing parallel-agent instruction with disjoint file ownership — auth implementation and test-only smoke tooling can run independently — cost if wrong: reconcile interface conflicts before final verification.

Ruling: Do not invent a Replit target, public origin, or secret-rotation evidence — ask for the missing target while finishing local work — cost if wrong: actual external release checks remain pending.

Ruling: Task24 may edit CSP in index.html before Task22 starts and then release ownership — these edits are independent and sequential, so waiting adds no protection — cost if wrong: reconcile shared HTML edits and rerun browser build checks.

Ruling: Omit frame-ancestors from CSP meta but retain it in required ingress response headers — browsers cannot enforce that directive via meta and plan explicitly permits only supported fallback policies — cost if wrong: provider response policy must be adjusted; no local claim of frame protection.

Ruling: For local and isolated Linux integrated verification, use this batch start5fb771b4 as the explicitly labeled local review base while separately checking original98 and recording the real origin/main merge-base failure — no PR exists and repairing inherited Python divergence violates the freeze — cost if wrong: actual PR integration checks still require reconciliation and cannot be claimed passed.

## 관련 근거

- [전체 계획](../plans/2026-09-09-full-learning-production.md)
- [UI·출시 계획](../plans/2026-09-09-learning-ui-release.md)
- [기존 11–15 실행 기록](2026-09-09-tasks-11-15.md)
- [기존 16–20 실행 기록](2026-09-10-tasks-16-20.md)
- [공개 범위](../../release-surface.md)
- [Secret incident ledger](../../security/secret-exposure-incident-ledger.md)
