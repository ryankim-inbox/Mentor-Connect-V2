{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "description": "Replit artifact stub. The API is the FastAPI app at the repo-root Python/ folder (see .replit-artifact/artifact.toml); artifacts/api-server/python/ is kept as legacy reference only."
}
